{
  "compilerOptions": {
    "target": "es2018",        // ← enable RegExp named captures
    "lib": ["esnext", "dom"],   // esnext already includes ES2018+, so this is fine
    "declaration": true,
    "declarationDir": "./types",
    // …any other options you need
  }
}